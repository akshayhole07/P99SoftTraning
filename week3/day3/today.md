1. Amazon EC    2 autoscalling 
3. IAM:      
2. aws cli  
4. S3: stores data as objects 
   --s3 storage classes
    --host the website statically in the s3 bucket
    -- route 53: domain name system (DNS) web service
    -- hostinger: hosting provider
5. aws lambda: serverless compute service that runs code in response to events and automatically manages the underlying compute resources
6. cloudTrail: a service that enables governance, compliance, operational auditing, and risk auditing of your AWS account
7. rds: relational database service that makes it easier to set up, operate, and scale a relational database in the cloud
9. DevOps: a set of practices that combines software development (Dev) and IT operations (Ops) to shorten the development lifecycle and provide continuous delivery with high software quality 